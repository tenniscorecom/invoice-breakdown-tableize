# 使い方

このツールの操作手順（実行する人向け）。エンジニアでなくてもこのページだけ読めば使えるようにする。

> このファイルはひな形です。`（ここを書く）` の部分をこのプロジェクトの内容に書き換えてください。

---

## このツールは何をするもの？

（ここを書く。例:「毎朝、売上CSVを読み込んで、Excelの日次レポートを自動で作ります」）

---

## 初回だけやること（1回でOK）

1. **`実行.bat` を1度動かす**（または `python main.py` を実行する）。`config.ini.example` から `config.ini` が作られて止まるので、
   中を開いて、フォルダの場所など（`（ここを書く）`）を自分の環境に合わせて書き換える


---

## 毎回の使い方

1. ターミナルで `実行.bat` を叩く（または `python main.py` を実行する）
2. 黒い画面が出て処理が進む（`（ここを書く：終わると何が出るか／どこに出力されるか）`）
3. `実行.bat` は `pause` を入れないので、画面は一瞬で閉じる（エラーならエラーコードが返る）

---

## うまくいかないとき

- 黒い画面に赤い英語（エラー名）が出たら、**[ERRORS.md](ERRORS.md)** でその名前を探して対処する
- 解決しないときは、**画面全体をスクリーンショット**して管理者に送る

### Access を使う場合

Access を更新する処理では、データベースと同じ場所の `backup` フォルダに控えができ、
既定で7日分残ります。おかしくなったら処理を止め、その中の日時が新しいものを
元の場所へ戻してください（コピーしてください）。
（Access を使わないプロジェクトではこの節を削除してください）

---

## よくある質問


**Q. 出力ファイルはどこ？**
A. `（ここを書く：config.ini の OUTPUT_FOLDER に設定した場所）`
